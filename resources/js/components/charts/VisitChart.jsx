import React from 'react';

const VisitChart = ({ activities }) => {
    return (
        <div className="chart visit-chart">
            📈 Graphique des visites (placeholder)
        </div>
    );
};

export default VisitChart;